# Style Usage Log

## 2026-03-02

- Applied `standalone-first-architecture` as the baseline for the refactor plan.
- Applied `pure-engine-policy` to keep masking logic out of Angular components.
- Applied `testing-placement` and `regex-governance` as the basis for Jest and Playwright setup.
- Applied `feature-folder-boundaries` to prepare a cleaner Angular core/features/shared split in the next refactor slice.
- Applied `dom-abstraction` while replacing manual node mutation with declarative Angular rendering.
- Applied `accessibility-copy-safety` by tying the primary copy action to masked output only.
