export const MASK_SAFETY_DECISIONS = Object.freeze({
  compromising: "compromising",
  safe: "safe",
  unsupported: "unsupported",
});

export const MASK_SAFETY_LIMITS = Object.freeze({
  batchSize: 128,
  maxAttemptsPerCandidate: 12,
});
