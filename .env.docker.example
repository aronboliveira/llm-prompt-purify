# Copy this file to `.env.docker` before running `docker compose --env-file .env.docker up --build`.
# Use the developer address and app password from `.sct/app-password.yml` for the SMTP values.

POSTGRES_DB=llm_prompt_purify
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

DEVELOPER_EMAIL_TO=aronprogramador@gmail.com
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_ENABLE_SSL=true
SMTP_USERNAME=aronprogramador@gmail.com
SMTP_PASSWORD=
SMTP_SENDER_EMAIL=aronprogramador@gmail.com
